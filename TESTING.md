# x-logview 测试用例文档

## 概述

本文档定义了 x-logview 项目的测试策略和测试用例，包括单元测试、接口测试和集成测试。

---

## 一、单元测试

### 1. 编码检测模块 (`internal/encoding`)

#### 1.1 UTF-8 检测

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| ENC-001 | 纯ASCII文本 | `[]byte("Hello World")` | `UTF8` |
| ENC-002 | 中文UTF-8文本 | `[]byte("你好世界")` | `UTF8` |
| ENC-003 | 混合字符 | `[]byte("Hello 你好")` | `UTF8` |
| ENC-004 | 空数据 | `[]byte("")` | `UTF8` |

#### 1.2 UTF-8 BOM 检测

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| ENC-010 | UTF-8 BOM文件 | `[]byte{0xEF, 0xBB, 0xBF, 0x48, 0x65, 0x6C, 0x6C, 0x6F}` | `UTF8BOM` |
| ENC-011 | 仅BOM头 | `[]byte{0xEF, 0xBB, 0xBF}` | `UTF8BOM` |

#### 1.3 UTF-16 检测

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| ENC-020 | UTF-16 LE | `[]byte{0xFF, 0xFE, 0x48, 0x00}` | `UTF16LE` |
| ENC-021 | UTF-16 BE | `[]byte{0xFE, 0xFF, 0x00, 0x48}` | `UTF16BE` |
| ENC-022 | UTF-16 LE 中文 | `[]byte{0xFF, 0xFE, 0x60, 0x4F, 0x7D, 0x59}` | `UTF16LE` |

#### 1.4 GBK 检测

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| ENC-030 | GBK中文 | GBK编码的"你好" | `GBK` |
| ENC-031 | GBK混合 | GBK编码的"Hello你好" | `GBK` |

#### 1.5 编码转换

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| ENC-040 | UTF-8转GBK | UTF-8中文，源编码UTF-8，目标GBK | GBK字节，无错误 |
| ENC-041 | GBK转UTF-8 | GBK中文，源编码GBK，目标UTF-8 | UTF-8字节，无错误 |
| ENC-042 | 同编码转换 | UTF-8数据，源=目标UTF-8 | 原始数据，无错误 |
| ENC-043 | UTF-16转UTF-8 | UTF-16 LE数据，目标UTF-8 | UTF-8字节，无错误 |
| ENC-044 | 空数据转换 | 空字节，任意编码 | 空字节，无错误 |

#### 1.6 解码器创建

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| ENC-050 | UTF-8 Reader | UTF-8 Reader，UTF8编码 | 返回原Reader |
| ENC-051 | GBK Reader | GBK Reader，GBK编码 | 返回解码后的Reader |
| ENC-052 | UTF-16 Reader | UTF-16数据，UTF16LE编码 | 返回解码后的Reader |

---

### 2. 缓冲区管理模块 (`pkg/buffer`)

#### 2.1 基本操作

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| BUF-001 | 创建缓冲区 | 默认配置 | 非空缓冲区实例 |
| BUF-002 | 设置文件大小 | 1024 | GetFileSize返回1024 |
| BUF-003 | 获取文件大小 | 无 | 返回设置的大小 |
| BUF-004 | 设置总行数 | 100 | GetTotalLines返回100 |
| BUF-005 | 获取总行数 | 无 | 返回设置的行数 |

#### 2.2 Chunk操作

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| BUF-010 | Put和Get | offset=0, data=[]byte("test") | Get(0)返回相同数据 |
| BUF-011 | Get不存在的offset | offset=999 | 返回nil, false |
| BUF-012 | 覆盖写入 | 同offset不同data | Get返回最新数据 |
| BUF-013 | 多个Chunk | 多个offset | 每个offset都能正确获取 |

#### 2.3 LRU淘汰

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| BUF-020 | 超过MaxChunks | MaxChunks=3，Put 4个 | 最早的被淘汰 |
| BUF-021 | 访问更新LRU | Get旧chunk | 旧chunk不被淘汰 |
| BUF-022 | Clear清空 | 调用Clear | 所有chunk被清除 |

#### 2.4 范围查询

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| BUF-030 | GetRange连续 | start=0, end=100 | 返回合并的数据 |
| BUF-031 | GetRange不连续 | start=0, end=100，中间有缺失 | 返回已有的数据 |

---

### 3. 缓存模块 (`pkg/cache`)

#### 3.1 基本操作

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| CACHE-001 | 创建缓存 | 有效目录 | 非空缓存实例 |
| CACHE-002 | Set和Get | key="test", value="data" | Get返回"data" |
| CACHE-003 | Get不存在的key | key="nonexistent" | 返回nil, false |
| CACHE-004 | Delete | 删除已存在的key | Get返回nil |
| CACHE-005 | Clear | 清空所有 | 所有Get返回nil |

#### 3.2 TTL过期

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| CACHE-010 | 设置短TTL | ttl=1秒 | 1秒后Get返回nil |
| CACHE-011 | 设置长TTL | ttl=1小时 | 短时间内Get返回值 |

#### 3.3 持久化

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| CACHE-020 | 写入磁盘 | Set后检查文件 | 文件存在 |
| CACHE-021 | 从磁盘加载 | 重启缓存 | 未过期数据仍可获取 |

---

### 4. 配置模块 (`pkg/config`)

#### 4.1 配置加载

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| CFG-001 | 默认配置 | 无配置文件 | 返回默认值 |
| CFG-002 | 加载配置文件 | 有效JSON文件 | 返回配置值 |
| CFG-003 | 无效配置文件 | 格式错误JSON | 返回默认配置 |
| CFG-004 | 部分配置 | 只设置部分字段 | 其他字段用默认值 |

#### 4.2 配置保存

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| CFG-010 | 保存配置 | 有效配置 | 文件成功写入 |
| CFG-011 | 保存后加载 | 保存后重新加载 | 返回相同配置 |

#### 4.3 全局配置

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| CFG-020 | GetConfig | 多次调用 | 返回相同实例 |

---

### 5. 文件服务模块 (`internal/file`)

#### 5.1 文件打开

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FILE-001 | 打开文本文件 | 有效文本路径 | 返回FileHandle，无错误 |
| FILE-002 | 打开二进制文件 | 有效二进制路径 | 返回FileHandle，file_type=binary |
| FILE-003 | 打开不存在的文件 | 无效路径 | 返回错误 |
| FILE-004 | 重复打开同一文件 | 同一路径两次 | 返回相同FileHandle |

#### 5.2 文件类型检测

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FILE-010 | 纯文本检测 | 纯ASCII内容 | file_type=text |
| FILE-011 | 二进制检测 | 含NULL字节 | file_type=binary |
| FILE-012 | 空文件检测 | 空内容 | file_type=text |

#### 5.3 编码检测

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FILE-020 | UTF-8检测 | UTF-8文件 | encoding=utf-8 |
| FILE-021 | GBK检测 | GBK文件 | encoding=gbk |
| FILE-022 | UTF-16检测 | UTF-16文件 | encoding=utf-16-le或utf-16-be |

#### 5.4 文件读取

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FILE-030 | 读取前N行 | start=0, num=10 | 返回10行，has_more=true |
| FILE-031 | 读取中间行 | start=10, num=10 | 返回正确行号 |
| FILE-032 | 读取超出范围 | start=1000, num=10 | 返回可用行，has_more=false |
| FILE-033 | 读取采样数据 | size=8192 | 返回最多8192字节 |

#### 5.5 文件监控

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FILE-040 | 文件修改通知 | 修改文件内容 | 触发FileUpdate回调 |
| FILE-041 | 文件删除通知 | 删除文件 | 设置loaded=false |
| FILE-042 | 关闭文件 | 调用Close | 停止监控，释放资源 |

#### 5.6 文件列表

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FILE-050 | 列出打开的文件 | 打开多个文件 | 返回所有文件信息 |
| FILE-051 | 空列表 | 无打开文件 | 返回空切片 |

---

### 6. 搜索服务模块 (`internal/search`)

#### 6.1 基本搜索

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SEARCH-001 | 简单文本搜索 | pattern="test" | 返回匹配位置 |
| SEARCH-002 | 无匹配搜索 | pattern="nonexistent" | 返回空结果 |
| SEARCH-003 | 多次匹配 | pattern出现在多行 | 返回所有匹配位置 |

#### 6.2 正则搜索

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SEARCH-010 | 简单正则 | pattern="test\d+", isRegex=true | 返回匹配位置 |
| SEARCH-011 | 无效正则 | pattern="[invalid", isRegex=true | 返回错误 |
| SEARCH-012 | 复杂正则 | pattern="(?i)hello", isRegex=true | 不区分大小写匹配 |

#### 6.3 大小写敏感

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SEARCH-020 | 大小写敏感 | pattern="Test", caseSensitive=true | 只匹配"Test" |
| SEARCH-021 | 大小写不敏感 | pattern="Test", caseSensitive=false | 匹配"test","TEST"等 |

#### 6.4 搜索取消

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SEARCH-030 | 取消搜索 | 调用Cancel | 搜索停止，无更多结果 |

#### 6.5 文本替换

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SEARCH-040 | 简单替换 | pattern="old", replace="new" | 返回替换次数和新内容 |
| SEARCH-041 | 正则替换 | pattern="\d+", replace="N", isRegex=true | 返回替换次数 |
| SEARCH-042 | 无匹配替换 | pattern="nonexistent", replace="new" | 返回0次替换 |
| SEARCH-043 | 多次替换 | pattern出现多次 | 返回正确替换次数 |

---

### 7. 格式化服务模块 (`internal/format`)

#### 7.1 JSON格式化

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FMT-001 | 有效JSON格式化 | `{"key":"value"}` | 格式化后的JSON |
| FMT-002 | 无效JSON格式化 | `{invalid}` | 返回错误 |
| FMT-003 | 嵌套JSON格式化 | `{"a":{"b":1}}` | 正确缩进 |

#### 7.2 JSON压缩

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FMT-010 | JSON压缩 | 格式化JSON | 压缩后的JSON |
| FMT-011 | 无效JSON压缩 | 无效JSON | 返回错误 |

#### 7.3 JSON验证

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FMT-020 | 有效JSON验证 | 有效JSON | 无错误 |
| FMT-021 | 无效JSON验证 | 无效JSON | 返回错误 |

#### 7.4 XML格式化

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FMT-030 | 有效XML格式化 | `<root><item/></root>` | 格式化后的XML |
| FMT-031 | 无效XML格式化 | `<root><item></root>` | 尝试修复或返回错误 |

#### 7.5 XML压缩

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FMT-040 | XML压缩 | 格式化XML | 压缩后的XML |

#### 7.6 XML验证

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| FMT-050 | 有效XML验证 | 有效XML | 无错误 |
| FMT-051 | 无效XML验证 | 无效XML | 返回错误 |

---

### 8. 会话管理模块 (`internal/session`)

#### 8.1 会话创建

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SESS-001 | 创建新会话 | id, fileInfo | 返回会话对象 |
| SESS-002 | 重复创建 | 同id | 更新已有会话 |

#### 8.2 会话获取

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SESS-010 | 获取存在会话 | 有效id | 返回会话，ok=true |
| SESS-011 | 获取不存在会话 | 无效id | 返回nil，ok=false |

#### 8.3 会话更新

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SESS-020 | 更新编辑器状态 | 新的EditorState | 状态已更新 |
| SESS-021 | 添加修改记录 | Change对象 | 修改已记录 |

#### 8.4 会话删除

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SESS-030 | 删除会话 | 有效id | 会话已删除 |
| SESS-031 | 删除临时文件 | 有临时文件的会话 | 临时文件已删除 |

#### 8.5 会话列表

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SESS-040 | 列出会话 | 多个会话 | 返回所有会话 |
| SESS-041 | 空列表 | 无会话 | 返回空切片 |

#### 8.6 按文件路径查找

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SESS-050 | 查找存在会话 | 有效路径 | 返回会话 |
| SESS-051 | 查找不存在会话 | 无效路径 | 返回nil |

#### 8.7 会话清理

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| SESS-060 | 清理过期会话 | maxAge=24小时 | 过期会话被删除 |

---

### 9. 自动保存模块 (`internal/session/autosave`)

#### 9.1 自动保存管理器

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| AUTO-001 | 创建管理器 | 有效配置 | 非空实例 |
| AUTO-002 | 启动管理器 | 调用Start | 开始定时保存 |
| AUTO-003 | 停止管理器 | 调用Stop | 停止定时保存 |

#### 9.2 会话注册

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| AUTO-010 | 注册会话 | id, filePath | 会话已注册 |
| AUTO-011 | 取消注册 | id | 会话已移除 |

#### 9.3 状态更新

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| AUTO-020 | 更新光标 | line, column | 光标已更新 |
| AUTO-021 | 更新滚动位置 | scrollTop, scrollLeft | 位置已更新 |
| AUTO-022 | 添加未保存修改 | key, data | 修改已添加 |
| AUTO-023 | 清除未保存修改 | id | 修改已清除 |

#### 9.4 状态获取

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| AUTO-030 | 获取存在会话 | 有效id | 返回状态 |
| AUTO-031 | 获取不存在会话 | 无效id | 返回nil |

#### 9.5 持久化

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| AUTO-040 | 保存到磁盘 | 调用saveAll | 文件已写入 |
| AUTO-041 | 从磁盘加载 | 启动管理器 | 状态已恢复 |
| AUTO-042 | 备份会话 | 保存时自动备份 | 备份文件存在 |

#### 9.6 清理

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| AUTO-050 | 清理过期会话 | maxAge | 过期会话已删除 |

---

### 10. 远程连接模块 (`internal/remote`)

#### 10.1 SSH连接

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| REMOTE-001 | 创建SSH连接 | 有效SSHConfig | 非空实例 |
| REMOTE-002 | SSH连接 | 有效配置 | 连接成功 |
| REMOTE-003 | SSH连接失败 | 无效配置 | 返回错误 |
| REMOTE-004 | SSH执行命令 | 有效命令 | 返回输出 |
| REMOTE-005 | SSH读取文件 | 有效路径 | 返回文件内容 |
| REMOTE-006 | SSH写入文件 | 有效路径+数据 | 文件已写入 |
| REMOTE-007 | SSH列出文件 | 有效目录 | 返回文件列表 |
| REMOTE-008 | SSH获取文件信息 | 有效路径 | 返回FileInfo |
| REMOTE-009 | SSH关闭连接 | 调用Close | 连接已关闭 |
| REMOTE-010 | SSH状态检查 | 调用IsConnected | 返回正确状态 |

#### 10.2 WSL连接

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| REMOTE-020 | 创建WSL连接 | 有效WSLConfig | 非空实例 |
| REMOTE-021 | WSL连接 | 有效配置 | 连接成功 |
| REMOTE-022 | WSL连接失败 | 非Windows系统 | 返回错误 |
| REMOTE-023 | WSL执行命令 | 有效命令 | 返回输出 |
| REMOTE-024 | WSL读取文件 | 有效路径 | 返回文件内容 |
| REMOTE-025 | WSL写入文件 | 有效路径+数据 | 文件已写入 |
| REMOTE-026 | WSL列出文件 | 有效目录 | 返回文件列表 |
| REMOTE-027 | WSL获取文件信息 | 有效路径 | 返回FileInfo |
| REMOTE-028 | WSL关闭连接 | 调用Close | 连接已关闭 |
| REMOTE-029 | WSL状态检查 | 调用IsConnected | 返回正确状态 |

#### 10.3 远程管理器

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| REMOTE-040 | 创建管理器 | 无 | 非空实例 |
| REMOTE-041 | 连接 | id, config | 连接成功 |
| REMOTE-042 | 重复连接 | 同id | 返回错误 |
| REMOTE-043 | 获取连接 | 有效id | 返回连接 |
| REMOTE-044 | 获取不存在连接 | 无效id | 返回错误 |
| REMOTE-045 | 断开连接 | 有效id | 连接已断开 |
| REMOTE-046 | 断开所有 | 调用DisconnectAll | 所有连接已断开 |
| REMOTE-047 | 列出连接 | 多个连接 | 返回所有id |
| REMOTE-048 | 状态检查 | 有效id | 返回正确状态 |

---

### 11. WebSocket模块 (`internal/ws`)

#### 11.1 Hub操作

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| WS-001 | 创建Hub | 无 | 非空Hub实例 |
| WS-002 | 注册Handler | MessageType, HandlerFunc | Handler已注册 |
| WS-003 | 启动Hub | 调用Run | Hub开始运行 |

#### 11.2 消息处理

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| WS-010 | 发送消息 | MessageType, payload | 消息已发送 |
| WS-011 | 接收消息 | 有效消息 | 调用对应Handler |
| WS-012 | 无效消息 | 格式错误JSON | 忽略消息 |
| WS-013 | 未知消息类型 | 未注册的MessageType | 无Handler调用 |

#### 11.3 客户端管理

| 测试ID | 测试用例 | 输入 | 预期输出 |
|--------|---------|------|---------|
| WS-020 | 客户端连接 | WebSocket连接 | 客户端已注册 |
| WS-021 | 客户端断开 | 关闭连接 | 客户端已移除 |
| WS-022 | 广播消息 | MessageType, payload | 所有客户端收到消息 |
| WS-023 | 发送错误 | id, error | 错误消息已发送 |

---

## 二、接口测试

### 1. HTTP API 接口

#### 1.1 健康检查

| 测试ID | 测试用例 | 请求 | 预期响应 |
|--------|---------|------|---------|
| API-001 | 健康检查 | `GET /health` | 200, `{"status":"ok"}` |

#### 1.2 文件API

| 测试ID | 测试用例 | 请求 | 预期响应 |
|--------|---------|------|---------|
| API-010 | 列出文件 | `GET /api/files` | 200, 文件列表JSON |
| API-011 | 空文件列表 | `GET /api/files` (无打开文件) | 200, `[]` |

#### 1.3 会话API

| 测试ID | 测试用例 | 请求 | 预期响应 |
|--------|---------|------|---------|
| API-020 | 列出会话 | `GET /api/sessions` | 200, 会话列表JSON |
| API-021 | 空会话列表 | `GET /api/sessions` (无会话) | 200, `[]` |

#### 1.4 CORS测试

| 测试ID | 测试用例 | 请求 | 预期响应 |
|--------|---------|------|---------|
| API-030 | OPTIONS请求 | `OPTIONS /health` | 200, CORS头 |
| API-031 | 跨域请求 | Origin头存在 | Access-Control-Allow-Origin: * |

---

### 2. WebSocket 接口

#### 2.1 文件操作

| 测试ID | 测试用例 | 消息类型 | 预期响应 |
|--------|---------|---------|---------|
| WSAPI-001 | 打开文件 | `file:open` | `file:open` + 文件信息 |
| WSAPI-002 | 关闭文件 | `file:close` | 成功关闭 |
| WSAPI-003 | 读取内容 | `file:content` | `file:content` + 内容数据 |
| WSAPI-004 | 文件更新通知 | 文件变化 | `file:update` + 文件信息 |

#### 2.2 搜索操作

| 测试ID | 测试用例 | 消息类型 | 预期响应 |
|--------|---------|---------|---------|
| WSAPI-010 | 开始搜索 | `search:start` | 多个`search:result` |
| WSAPI-011 | 取消搜索 | `search:cancel` | 搜索停止 |
| WSAPI-012 | 文本替换 | `search:replace` | `search:replace` + 替换结果 |

#### 2.3 编码操作

| 测试ID | 测试用例 | 消息类型 | 预期响应 |
|--------|---------|---------|---------|
| WSAPI-020 | 检测编码 | `encoding:detect` | `encoding:detect` + 编码信息 |
| WSAPI-021 | 转换编码 | `encoding:convert` | `encoding:convert` + 转换结果 |

#### 2.4 格式化操作

| 测试ID | 测试用例 | 消息类型 | 预期响应 |
|--------|---------|---------|---------|
| WSAPI-030 | 格式化JSON | `format:json` | `format:json` + 格式化结果 |
| WSAPI-031 | 格式化XML | `format:xml` | `format:xml` + 格式化结果 |

#### 2.5 会话操作

| 测试ID | 测试用例 | 消息类型 | 预期响应 |
|--------|---------|---------|---------|
| WSAPI-040 | 保存会话 | `session:save` | `session:save` + 状态 |
| WSAPI-041 | 恢复会话 | `session:restore` | `session:restore` + 会话数据 |
| WSAPI-042 | 同步状态 | `state:sync` | 状态已同步 |
| WSAPI-043 | 更新光标 | `cursor:update` | 光标已更新 |

#### 2.6 远程连接操作

| 测试ID | 测试用例 | 消息类型 | 预期响应 |
|--------|---------|---------|---------|
| WSAPI-050 | 连接远程 | `remote:connect` | `remote:connect` + 状态 |
| WSAPI-051 | 断开远程 | `remote:disconnect` | `remote:disconnect` + 状态 |
| WSAPI-052 | 列出远程 | `remote:list` | `remote:list` + 连接列表 |
| WSAPI-053 | 执行远程命令 | `remote:exec` | `remote:exec` + 输出 |

#### 2.7 自动保存操作

| 测试ID | 测试用例 | 消息类型 | 预期响应 |
|--------|---------|---------|---------|
| WSAPI-060 | 注册自动保存 | `autosave:save` | `autosave:save` + 状态 |
| WSAPI-061 | 恢复自动保存 | `autosave:restore` | `autosave:restore` + 状态 |
| WSAPI-062 | 更新自动保存 | `autosave:update` | `autosave:update` + 状态 |

#### 2.8 心跳

| 测试ID | 测试用例 | 消息类型 | 预期响应 |
|--------|---------|---------|---------|
| WSAPI-070 | Ping | `ping` | `pong` |

---

## 三、集成测试

### 1. 文件打开流程

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-001 | 打开文本文件 | 1. 发送file:open<br>2. 等待响应<br>3. 读取内容 | 文件信息正确，内容可读 |
| INT-002 | 打开大文件 | 1. 打开GB级文件<br>2. 读取部分内容<br>3. 滚动加载 | 响应时间<1秒，内容正确 |
| INT-003 | 打开二进制文件 | 1. 发送file:open<br>2. 检查file_type | file_type=binary |
| INT-004 | 打开不存在文件 | 1. 发送file:open<br>2. 检查错误 | 返回错误信息 |

### 2. 文件监控流程

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-010 | 文件修改通知 | 1. 打开文件<br>2. 修改文件<br>3. 等待通知 | 收到file:update |
| INT-011 | 文件删除通知 | 1. 打开文件<br>2. 删除文件<br>3. 等待通知 | loaded=false |
| INT-012 | Follow模式 | 1. 打开文件<br>2. 启用Follow<br>3. 追加内容 | 自动显示新内容 |

### 3. 搜索替换流程

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-020 | 基本搜索 | 1. 打开文件<br>2. 发送搜索<br>3. 收集结果 | 返回所有匹配 |
| INT-021 | 正则搜索 | 1. 打开文件<br>2. 正则搜索<br>3. 收集结果 | 返回匹配 |
| INT-022 | 取消搜索 | 1. 开始搜索<br>2. 发送取消<br>3. 检查结果 | 搜索停止 |
| INT-023 | 文本替换 | 1. 打开文件<br>2. 发送替换<br>3. 重新读取 | 内容已替换 |
| INT-024 | 全部替换 | 1. 打开文件<br>2. 替换全部<br>3. 检查结果 | 所有匹配已替换 |

### 4. 编码处理流程

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-030 | 检测编码 | 1. 打开文件<br>2. 检测编码 | 返回正确编码 |
| INT-031 | 转换编码 | 1. 打开文件<br>2. 转换编码<br>3. 检查结果 | 编码已转换 |
| INT-032 | 切换编码显示 | 1. 打开文件<br>2. 切换编码<br>3. 检查显示 | 显示正确 |

### 5. 会话管理流程

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-040 | 保存会话 | 1. 打开文件<br>2. 修改状态<br>3. 保存会话 | 会话已保存 |
| INT-041 | 恢复会话 | 1. 保存会话<br>2. 关闭应用<br>3. 恢复会话 | 状态已恢复 |
| INT-042 | 自动保存 | 1. 打开文件<br>2. 等待自动保存<br>3. 检查文件 | 自动保存文件存在 |
| INT-043 | 恢复自动保存 | 1. 自动保存<br>2. 重启应用<br>3. 恢复状态 | 状态已恢复 |

### 6. 远程连接流程

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-050 | SSH连接 | 1. 配置SSH<br>2. 连接<br>3. 执行命令 | 命令执行成功 |
| INT-051 | SSH文件操作 | 1. SSH连接<br>2. 读取文件<br>3. 检查内容 | 文件内容正确 |
| INT-052 | WSL连接 | 1. 配置WSL<br>2. 连接<br>3. 执行命令 | 命令执行成功 |
| INT-053 | 断开远程 | 1. 连接<br>2. 断开<br>3. 检查状态 | 连接已断开 |

### 7. 格式化流程

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-060 | JSON格式化 | 1. 打开JSON文件<br>2. 格式化<br>3. 检查结果 | JSON已格式化 |
| INT-061 | JSON压缩 | 1. 打开JSON文件<br>2. 压缩<br>3. 检查结果 | JSON已压缩 |
| INT-062 | XML格式化 | 1. 打开XML文件<br>2. 格式化<br>3. 检查结果 | XML已格式化 |
| INT-063 | XML压缩 | 1. 打开XML文件<br>2. 压缩<br>3. 检查结果 | XML已压缩 |

### 8. 高亮显示流程

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-070 | 搜索高亮 | 1. 搜索<br>2. 检查高亮 | 匹配项高亮 |
| INT-071 | 选中文本高亮 | 1. 选中文本<br>2. 检查高亮 | 相同文本高亮 |
| INT-072 | 当前匹配高亮 | 1. 搜索<br>2. 切换当前<br>3. 检查高亮 | 当前匹配特殊高亮 |

### 9. 数字提示流程

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-080 | 十六进制显示 | 1. 选中数字<br>2. 检查右侧面板 | 显示十六进制 |
| INT-081 | 时间戳显示 | 1. 选中时间戳<br>2. 检查右侧面板 | 显示时间 |
| INT-082 | 毫秒时间戳 | 1. 选中毫秒时间戳<br>2. 检查右侧面板 | 显示时间 |

### 10. 流式加载流程

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-090 | 初始加载 | 1. 打开大文件<br>2. 检查加载 | 快速显示部分内容 |
| INT-091 | 滚动加载 | 1. 滚动到底部<br>2. 等待加载 | 自动加载更多 |
| INT-092 | 行号显示 | 1. 未加载完成<br>2. 检查行号 | 未加载行显示... |
| INT-093 | 行号更新 | 1. 加载完成<br>2. 检查行号 | 行号已更新 |

### 11. 错误处理流程

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-100 | 无效WebSocket消息 | 1. 发送无效JSON<br>2. 检查行为 | 忽略消息，不断开 |
| INT-101 | 文件打开失败 | 1. 发送无效路径<br>2. 检查响应 | 返回错误信息 |
| INT-102 | 搜索失败 | 1. 无效正则<br>2. 检查响应 | 返回错误信息 |
| INT-103 | 连接断开 | 1. 关闭连接<br>2. 检查清理 | 资源已清理 |

### 12. 性能测试

| 测试ID | 测试用例 | 测试步骤 | 预期结果 |
|--------|---------|---------|---------|
| INT-110 | 大文件打开 | 1. 打开10GB文件<br>2. 测量时间 | <1秒 |
| INT-111 | 大文件滚动 | 1. 滚动10GB文件<br>2. 检查流畅度 | 流畅，无卡顿 |
| INT-112 | 大文件搜索 | 1. 搜索10GB文件<br>2. 检查响应 | 异步执行，不阻塞 |
| INT-113 | 内存使用 | 1. 打开大文件<br>2. 监控内存 | 内存使用可控 |
| INT-114 | 并发连接 | 1. 多个WebSocket连接<br>2. 并发操作 | 所有连接正常 |

---

## 四、测试数据

### 1. 测试文件

| 文件名 | 类型 | 大小 | 编码 | 用途 |
|--------|------|------|------|------|
| test-small.txt | 文本 | 1KB | UTF-8 | 基本功能测试 |
| test-medium.txt | 文本 | 100MB | UTF-8 | 中等文件测试 |
| test-large.txt | 文本 | 10GB | UTF-8 | 大文件测试 |
| test-gbk.txt | 文本 | 1MB | GBK | 编码测试 |
| test-utf16.txt | 文本 | 1MB | UTF-16 | 编码测试 |
| test-binary.bin | 二进制 | 10MB | N/A | 二进制测试 |
| test.json | JSON | 1MB | UTF-8 | JSON格式化测试 |
| test.xml | XML | 1MB | UTF-8 | XML格式化测试 |
| test.log | 日志 | 1GB | UTF-8 | 日志查看测试 |

### 2. 测试数据生成

```bash
# 生成小文件
echo "Hello World" > test-small.txt

# 生成中等文件
for i in {1..1000000}; do echo "Line $i: This is a test line with some content"; done > test-medium.txt

# 生成大文件
for i in {1..100000000}; do echo "Line $i: This is a test line with some content"; done > test-large.txt

# 生成GBK文件
iconv -f UTF-8 -t GBK test-small.txt > test-gbk.txt

# 生成UTF-16文件
iconv -f UTF-8 -t UTF-16LE test-small.txt > test-utf16.txt

# 生成二进制文件
dd if=/dev/urandom of=test-binary.bin bs=1M count=10
```

---

## 五、测试环境

### 1. 环境要求

- Go 1.21+
- Node.js 18+
- 操作系统: macOS, Linux, Windows

### 2. 测试工具

- Go testing 包
- Jest (前端测试)
- WebSocket 测试客户端
- curl (HTTP API测试)

### 3. 测试配置

```json
{
  "buffer": {
    "initial_size": 65536,
    "max_size": 268435456,
    "chunk_size": 4096,
    "max_chunks": 1000
  },
  "server": {
    "port": 8090,
    "hostname": "localhost"
  },
  "session": {
    "auto_save": true,
    "auto_save_interval": 5,
    "restore_state": true
  }
}
```

---

## 六、测试执行

### 1. 单元测试

```bash
# 运行所有单元测试
go test ./...

# 运行特定模块测试
go test ./internal/encoding/...
go test ./internal/file/...
go test ./internal/search/...

# 运行带覆盖率的测试
go test -cover ./...

# 运行带详细输出的测试
go test -v ./...
```

### 2. 集成测试

```bash
# 启动服务器
./bin/x-logview-server -port 8090 &

# 运行集成测试
go test -tags=integration ./test/integration/...
```

### 3. 性能测试

```bash
# 运行基准测试
go test -bench=. ./...

# 运行内存分析
go test -memprofile=mem.out ./...

# 运行CPU分析
go test -cpuprofile=cpu.out ./...
```

---

## 七、测试报告

### 1. 报告内容

- 测试执行时间
- 通过/失败/跳过的测试数
- 覆盖率报告
- 性能指标
- 错误详情

### 2. 报告格式

- JUnit XML
- HTML 报告
- 覆盖率报告 (HTML)
